# Shared baseline
NODE_ENV=development

# Port allocation is defined in configs/ports.json as BASE + offset
# BASE=3000

# API (BASE + 0)
API_PORT=3000

# UI (BASE + 2173)
UI_PORT=5173

# Bot (BASE + 1)
BOT_PORT=3001

# Scheduler (BASE + 2)
SCHEDULER_PORT=3002
